@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>@ViewData["Title"] - VizinhApp</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />

    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" crossorigin="anonymous" />

    <!-- CSS do site -->
    <link rel="stylesheet" href="~/css/site.css" />
    @RenderSection("Styles", required: false)

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        html, body {
            height: 100%;
            width: 100%;
            overflow-x: hidden;
        }
        
        .d-flex {
            display: flex;
            flex-direction: row;
            min-height: 100vh;
        }
        
        main {
            flex: 1;
            width: 100%;
            overflow-y: auto;
        }
    </style>
</head>
<body>
    <div class="d-flex">
        <!-- Sidebar -->
        <nav class="sidebar bg-light p-3 shadow-sm">
            <div class="text-center mb-4">
                <h5 class="fw-bold text-primary">🏢 VizinhApp</h5>
                <p class="small text-muted">Gestão Inteligente</p>
            </div>

            <ul class="nav flex-column">
                <li class="nav-item mb-2">
                    <a class="nav-link" asp-controller="Usuario" asp-action="InterfaceSindico" title="Dashboard"><i class="bi bi-bar-chart"></i> <span class="d-lg-inline d-none">Dashboard</span></a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link" asp-controller="FinanceiroSindico" asp-action="Index" title="Financeiro"><i class="bi bi-currency-dollar"></i> <span class="d-lg-inline d-none">Financeiro</span></a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link" asp-controller="Vistoria" asp-action="Index" title="Vistorias"><i class="bi bi-clipboard-check"></i> <span class="d-lg-inline d-none">Vistorias</span></a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link" asp-controller="Manutencao" asp-action="Index" title="Manutenções"><i class="bi bi-wrench-adjustable"></i> <span class="d-lg-inline d-none">Manutenções</span></a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link" asp-controller="Comunicacao" asp-action="Index" title="Comunicação"><i class="bi bi-bell"></i> <span class="d-lg-inline d-none">Comunicação</span></a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link" asp-controller="Acesso" asp-action="Index" title="Controle de Acesso"><i class="bi bi-shield-lock"></i> <span class="d-lg-inline d-none">Controle de Acesso</span></a>
                </li>
                <li class="nav-item mb-2">
                    <a class="nav-link" asp-controller="Relatorios" asp-action="Index" title="Relatórios"><i class="bi bi-graph-up"></i> <span class="d-lg-inline d-none">Relatórios</span></a>
                </li>
            </ul>

            <div class="mt-auto pt-3 border-top">
                <p class="text-muted small mb-1">
                    <i class="bi bi-person-badge"></i> <span class="d-lg-inline d-none">@HttpContextAccessor.HttpContext.Session.GetString("NomeUsuario")</span><br/>
                    <span class="text-secondary d-lg-inline d-none">Síndico</span>
                </p>
                <form asp-controller="Usuario" asp-action="Logout" method="post">
                    <button class="btn btn-outline-danger btn-sm w-100"><i class="bi bi-box-arrow-right"></i> <span class="d-lg-inline d-none">Sair</span></button>
                </form>
            </div>
        </nav>

        <!-- Conteúdo principal -->
        <main class="flex-fill">
            @RenderBody()
        </main>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>